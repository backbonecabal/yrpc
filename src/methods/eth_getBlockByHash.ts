import { EthGetBlockByHash } from "../generated-typings";

const eth_getBlockByHash: EthGetBlockByHash = (blockHash, includeTransactions) => {
  return Promise.resolve();
};

export default eth_getBlockByHash;
