import { EthGetTransactionReceipt } from "../generated-typings";

const eth_getTransactionReceipt: EthGetTransactionReceipt = (transactionHash) => {
  return Promise.resolve();
};

export default eth_getTransactionReceipt;
