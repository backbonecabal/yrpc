import { EthGetBlockByNumber } from "../generated-typings";

const eth_getBlockByNumber: EthGetBlockByNumber = (blockNumber, includeTransactions) => {
  return Promise.resolve();
};

export default eth_getBlockByNumber;
