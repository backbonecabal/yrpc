import { EthGetTransactionCount } from "../generated-typings";

const eth_getTransactionCount: EthGetTransactionCount = (address, blockNumber) => {
  return Promise.resolve();
};

export default eth_getTransactionCount;
