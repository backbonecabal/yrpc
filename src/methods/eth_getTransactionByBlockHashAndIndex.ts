import { EthGetTransactionByBlockHashAndIndex } from "../generated-typings";

const eth_getTransactionByBlockHashAndIndex: EthGetTransactionByBlockHashAndIndex = (blockHash, index) => {
  return Promise.resolve(null);
};

export default eth_getTransactionByBlockHashAndIndex;
