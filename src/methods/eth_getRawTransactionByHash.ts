import { EthGetRawTransactionByHash } from "../generated-typings";

const eth_getRawTransactionByHash: EthGetRawTransactionByHash = (transactionHash) => {
  return Promise.resolve();
};

export default eth_getRawTransactionByHash;
