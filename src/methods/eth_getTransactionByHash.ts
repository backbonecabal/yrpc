import { EthGetTransactionByHash } from "../generated-typings";

const eth_getTransactionByHash: EthGetTransactionByHash = (transactionHash) => {
  return Promise.resolve();
};

export default eth_getTransactionByHash;
